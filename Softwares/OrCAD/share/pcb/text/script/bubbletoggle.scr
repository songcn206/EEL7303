# $Header: /vobs/psd_backend_data/data/scripts/bubbletoggle.scr,v 1.1 1900/05/25 15:48:06 fxf Prealpha $

# Example of using a macro to control the bubble field option in
# the add connect command

# assign to alias: alias F3 replay bubbletoggle
# each time the function key F3 is hit the field advances
# to the next setting. 
# Requires add connect to be active.

settoggle buggle_type Off "Hug preferred" "Shove preferred"
setwindow form.mini
FORM mini bubble_space $buggle_type
setwindow pcb
